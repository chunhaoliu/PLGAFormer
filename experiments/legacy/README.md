# Legacy experiment index

The numbered directories below are retained only where the canonical study or
result contract still needs a compatibility boundary. They do not define a
second paper route. Inactive source directories listed below are preserved in
the external archive with hash manifests.

| Legacy path | Role |
|---|---|
| `exp1_sota/`, `exp2_ablation/`, `exp6_efficiency/` | Thin compatibility boundaries for the canonical studies |
| `exp3_robustness/`, `exp4_physics_consistency/` | Inactive source archived; use `generalization_robustness/` or `mechanism_analysis/` |
| `exp5_missing_data/`, `exp7_longterm/` | Inactive development studies archived outside the repository |
| `exp5_ood_dynamics/` | Preserved robustness result root, not an active source directory |

Archived source remains recoverable from
`D:\Research\HGV_Code\Archive\HGVTP_PLGAformer-main\tracked_source`; the
manifest records the repository-relative path and SHA-256 before removal.
